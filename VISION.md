# Meridian Vision

Meridian の最終ゴールは、世界中のプロダクトチームが信頼して使えるデザインシステムと、それを土台にプロレベルの UI/UX 画面設計を AI と共同で生成できる環境を作ることです。

Meridian は単なるコンポーネント集ではありません。デザイナー、エンジニア、プロダクトマネージャー、非デザイナーが、AI との壁打ちを通じて「なぜこの画面なのか」を理解しながら、実装可能でアクセシブルな画面を作るための設計基盤です。

## North Star

Meridian は、根拠に基づく画面設計を誰でも扱えるようにします。

ユーザーが自然言語で目的や課題を伝えると、Meridian はデザインシステムの原則、トークン、コンポーネント、パターン、アクセシビリティ要件に基づいて、複数の UI/UX 案を提案します。それぞれの案には、情報設計、コンポーネント選定、インタラクション、ユーザー体験上の意図、トレードオフが説明されます。

最終的にユーザーは、単発の画面だけでなく、複数画面を横断するプロトタイプとして体験を検証できるようになります。

## Product Goals

1. 世界的に使えるデザインシステムに育てる。
   Meridian はテーマ、密度、アクセシビリティ、多言語、実装互換性を前提にした、プロダクト UI 向けの堅牢なシステムであるべきです。

2. AI が設計判断できる構造化された知識ベースを作る。
   原則、トークン、コンポーネント、パターン、テンプレート、禁止ルールを AI が読める形で管理し、生成結果が Meridian の設計思想から外れないようにします。

3. デザイナーも非デザイナーも、AI との対話でプロ品質の画面を生成できるようにする。
   ただ画面を出力するのではなく、ユーザーの目的、対象ユーザー、利用文脈、情報量、業務フロー、制約を確認し、設計根拠を明示します。

4. 単発画面と複数画面プロトタイプの両方を扱う。
   画面単位の設計だけでなく、ナビゲーション、状態遷移、エラー、空状態、権限差分、オンボーディングなどを含むプロトタイプ生成を目指します。

5. 生成後の評価と改善ループを持つ。
   AI が生成した UI を、トークン準拠、アクセシビリティ、情報設計、レスポンシブ品質、コンポーネント適合率、説明可能性で検証し、改善できるようにします。

## Design Generation Principles

Meridian の AI 生成は、次の原則に従います。

- 根拠から始める。
  画面を描く前に、ユーザーの目的、主要タスク、情報優先度、制約、成功条件を整理します。

- 複数案を提案する。
  1 案だけを出すのではなく、方向性の異なる案を提示し、推奨案とその理由を明確にします。

- Meridian の部品だけで組み立てる。
  色、余白、角丸、モーション、コンポーネント、レイアウトパターンは Meridian の定義を使います。必要な部品がない場合は、追加すべき理由を明示します。

- アクセシビリティを初期条件にする。
  コントラスト、キーボード操作、フォーカス、読み上げ、状態表現は後付けではなく、設計段階から満たします。

- 画面ではなく体験を設計する。
  成功状態だけでなく、初回利用、空状態、ローディング、エラー、確認、取り消し、権限不足なども含めます。

- 説明可能な UI にする。
  生成された画面には、なぜその情報構造、コンポーネント、導線、文言、状態設計になったのかを説明できる必要があります。

## Near-Term Priorities

### 1. AI generation schema

`ScreenSpec`、`FlowSpec`、`PrototypeSpec` のような中間仕様を定義します。

これは、チャット内容から直接 HTML や React を生成しないための重要な層です。目的、ユーザー、タスク、画面構成、使用コンポーネント、設計根拠、代替案、推奨案、検証項目を構造化します。

### 2. Pattern knowledge base

既存の Patterns / Templates を、AI が設計判断に使える知識ベースへ拡張します。

各パターンには、適用条件、避ける条件、情報設計、主要コンポーネント、失敗しやすい点、アクセシビリティ要件、代替パターンを持たせます。

### 3. Production component package

React パッケージを実用段階へ進めます。

ドキュメント上のコンポーネントと実装コンポーネントが一致していないと、AI が生成した画面を実際のプロダクトに持ち込めません。まずは Button、Text Field、Dialog、Tabs、Table、Sidebar などのコアコンポーネントから着手します。

### 4. Generation quality gates

`design-lint` を、AI 生成画面の品質ゲートに育てます。

トークン違反やアクセシビリティ違反だけでなく、コンポーネント適合率、レスポンシブ崩れ、説明不足、状態設計の欠落を検出できるようにします。

### 5. Public documentation and global readiness

外部ユーザーに伝わる英語ドキュメント、導入ガイド、設計原則、実装例を整備します。

世界的に使えるデザインシステムにするには、使い始めやすさ、信頼できる品質基準、貢献しやすい構造が必要です。

## Success Measures

Meridian が目指す成功は、見た目の美しさだけでは測りません。

- 非デザイナーが要件を伝えるだけで、根拠付きの画面案を比較できる。
- AI が生成した UI が Meridian のトークン、コンポーネント、ルールに準拠している。
- 単発画面だけでなく、複数画面のプロトタイプとして体験を検証できる。
- デザイナーが生成結果を「修正の叩き台」ではなく「議論に耐える案」として扱える。
- エンジニアが生成結果を実装へ移しやすい。
- アクセシビリティ、レスポンシブ、状態設計が初期生成から考慮されている。
- Meridian の知識ベースが利用とフィードバックによって改善され続ける。

## Non-Goals For Now

- すべての UI ジャンルを最初から対象にしない。
  まずは SaaS、業務ツール、管理画面、AI ワークスペース、設定、分析、チーム管理など、Meridian の強みが出るプロダクト UI に集中します。

- 見た目だけのランディングページ生成を主目的にしない。
  Meridian は意思決定、業務、操作、状態管理を含むアプリケーション UI を主戦場にします。

- AI の自由生成を無制限に許さない。
  生成は Meridian の設計原則、トークン、コンポーネント、パターン、品質ゲートの範囲内で行います。

## Operating Principle

新しい機能、コンポーネント、テンプレート、AI 生成機能を追加するときは、次の問いに答えられることを条件にします。

1. これはユーザーがより良い UI/UX 判断をする助けになるか。
2. Meridian の原則、トークン、コンポーネント体系と整合しているか。
3. AI がこの知識を再利用できる形で記録されているか。
4. 生成結果の品質を検証できるか。
5. 世界中のプロダクトチームが使っても破綻しないか。

Meridian は、速く画面を作るためだけの道具ではありません。設計判断を透明にし、品質を再現可能にし、より多くの人がプロダクトデザインに参加できるようにするための基盤です。
